# Security Policy

Report vulnerabilities with GitHub private security advisories, not public
issues. Never include tokens or private voice content in reports. The MCP HTTP
transport must authenticate non-loopback deployments; write tools require
explicit scopes and audit coverage before release.

The project is pre-1.0; only the latest default branch receives security fixes.
