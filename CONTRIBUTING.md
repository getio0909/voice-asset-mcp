# Contributing

Use Go 1.26.5 or newer in the 1.26 release line. Create focused changes, run `make verify`, and use
Conventional Commits. Pull requests must describe tool/schema changes, required
Server contract versions, scope implications, and validation performed. Never
include access tokens, transcript fixtures containing personal data, or direct
database access.
